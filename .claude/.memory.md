PROJECT PLAN - Reconcile managed subtitle maintenance
LAST UPDATED: 2026-08-31

GOAL:
Detect and safely regenerate confidently mislabeled managed subtitles in every configured language, prevent completed cue repairs from remaining active indefinitely, and preserve the captured production evidence as regression fixtures.

BRANCH:
- `fix/reconcile-managed-subtitle-maintenance`, created from freshly fetched `origin/main` at `9ed8f2d4729e58cb2149bbb1dfe11cbb59f7f06b`.
- `.codex-remote-attachments/` remains user-owned and excluded from staging.

IMPLEMENTED:
- Existing-library discovery validates the union of `LANGUAGES` and `CLEANUP_LANGUAGES`; AI cue/format repair remains limited to `CLEANUP_LANGUAGES`.
- Outside the AI scope, mutation requires valid SRT structure, usable completeness evidence, enough dialogue, a different detected language, and configured high confidence.
- Confident mismatches follow the normal quarantine, Bazarr synchronization, wanted-item regeneration, and validation flow. Ambiguous, short, stale, receipt-owned, forced/commentary, and unrelated extracted files remain untouched.
- Repair terminal publication passes the required circuit reason, contains unexpected secondary-bookkeeping failures, and releases maintenance scan children once.
- Maintenance scans retain their active context until terminal status is persisted, with same-process retry for both child and zero-child scans after transient storage failures.
- Maintenance completion restores its active row after snapshot failure and deduplicates the terminal history event on retry.
- Validation and cleanup use the caller-captured subtitle hash, so a concurrently replaced sidecar cannot inherit stale validation or be removed.
- Typed language-validation outcomes appear in Work/Recent without exposing paths or subtitle text.
- The real Shameless en/et/sv triplet and all 22 MissingRepair receipts (with 21 available payloads and one intentional missing payload) are covered by deterministic tests.

VERIFIED:
- 157 focused Python tests and 333 full-discovery tests passed; one platform-conditional test skipped as expected.
- Python compile checks passed.
- Frontend TypeScript, 19 Vitest tests, production build, and 16 Playwright visual comparisons passed.
- The `bazarr-autotranslate:reconcile-maintenance-rc` image built and its packaged production imports/smoke check passed.
- Final severity review is clean: no P0-P2 findings remain after both concurrency/lifecycle findings were fixed and covered by regressions.

NEXT:
- The reviewed implementation is committed locally on `fix/reconcile-managed-subtitle-maintenance`.
- Do not push, open a PR, deploy, or modify live TrueNAS data without separate authorization.

ROLLBACK:
- Revert the single focused commit. No schema, HTTP API, configuration, or live TrueNAS changes are involved.
