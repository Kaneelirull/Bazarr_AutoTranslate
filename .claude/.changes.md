# .changes.md — project change log
# Maintained by git-deploy agent. Newest entries at top.

2026-04-19 | parse S/E from subtitle source filename instead of Bazarr API dict
  - regex S01E02 pattern applied to source_path after it is resolved in process_item()
  - _item_title() simplified back to series name only; S/E appended separately
  - removes dependency on wanted-endpoint item dict which does not carry episode fields

