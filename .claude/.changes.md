2026-08-17 | prefer extracted embedded subtitle sources
  - validate SubExtractorr schema v1 receipts and select embedded sources before Bazarr
  - merge rolling duplicate cues atomically before translation
  - publish Lingarr output under canonical target-language filenames
  - preserve extracted sources outside target readiness and pruning
2026-08-18 | recover translations from multiple quarantine attempts
  - validate changed sidecars from terminal Lingarr jobs with submission-bound provenance
  - run durable donor-first quarantine ensemble recovery before full regeneration
  - preserve active donor artifacts, partial cue recoveries, and restart-safe attempt sets
  - attach maintenance validation to matching submission or retry identities
2026-08-18 | migrate operator UI to React
  - replace the Status, Manual review, and Logs renderers with React 19 and TypeScript
  - preserve URLs, API contracts, CSP, accessibility behavior, theme tokens, polling, and manual-action safeguards
  - add a locked Vite multi-entry build to Docker and CI while keeping Node out of the runtime image
  - cover core refresh, stale-data, action, filtering, pagination, and tooltip behavior with Vitest and Python integration tests
2026-08-18 | simplify and harden the operator UI
  - consolidate Status into Overview, Work with Recent, and an attention-aware Health & history disclosure
  - standardize responsive gutters, panel bodies, mobile card tables, touch targets, and progressive secondary filters
  - keep Manual Review interactive during polling while protecting mutation refreshes and preserving focus/filter/expanded state
  - add 16 deterministic Playwright baselines across Status, Manual Review, and Logs in both themes and desktop/mobile viewports
  - verify 19 Vitest tests, 61 focused Python tests, 308 containerized Python tests, production build, Docker image, and seven HTTP routes/assets
2026-08-18 | stabilize cross-platform visual regression checks
  - separate Windows and Linux Playwright baselines instead of weakening the screenshot threshold
  - generate and inspect Linux baselines with the pinned Playwright 1.55.1 Noble container
  - verify all 16 comparisons on both Windows and Linux plus TypeScript and 19 Vitest tests
2026-08-20 | canonicalize embedded translation outputs
  - drop numeric extraction collision suffixes from canonical Bazarr target names while preserving HI and SDH
  - discover both Lingarr embedded-output filename layouts and atomically publish the changed candidate
  - self-heal exact submission-backed orphan outputs without adopting stale, unmatched, or receipt-owned files
  - atomically terminalize completed jobs with missing output and schedule durable whole-file retries
