# .changes.md — project change log
# Maintained by git-deploy agent. Newest entries at top.

2026-04-20 | feat: tune timeout estimates, add hard cap, log Lingarr queue on timeout, 30s startup wait
  - raised POLL_TIMEOUT default from 600 to 900s
  - updated _estimate_timeout(): rate 1.1→1.8 s/line, multiplier 1.2→1.3, hard cap = max(POLL_TIMEOUT, CHECK_INTERVAL-60)
  - on timeout: check disk first → [OK] if file exists; log Lingarr queue count if true timeout
  - updated startup banner to show floor/cap values
  - added 30s startup wait before first cycle for services to become ready

