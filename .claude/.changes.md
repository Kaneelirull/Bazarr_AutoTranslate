# .changes.md — project change log
# Maintained by git-deploy agent. Newest entries at top.

2026-04-19 | fix: dual-check subtitle completion via available path OR missing_subtitles removal
  - added fetch_sub_status() helper returning available_code2s (path set) and missing_code2s in one API call
  - wait_for_subtitle() now fires as soon as target_lang is in available OR not in missing, catching imports where path isn't set yet

