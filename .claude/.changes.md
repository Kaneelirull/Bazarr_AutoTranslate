# .changes.md — project change log
# Maintained by git-deploy agent. Newest entries at top.

2026-04-19 | Add pre-submit subtitle re-check and post-sleep Lingarr drain wait
  - process_item(): re-checks Bazarr subtitle availability right before submit_translate; skips submission and counts as completed if target subtitle already present
  - main(): after CHECK_INTERVAL sleep, polls lingarr_active_count() until 0 (capped at 2×CHECK_INTERVAL with a warning) before starting next cycle

